import 'package:flutter/material.dart';

class AppColors {
  // Paleta color PISCINA (cyan verdoso, no celeste)
  static const Color primary = Color(0xFF26C6DA);        // Cyan piscina (AppBar, botones)
  static const Color primaryDark = Color(0xFF00ACC1);     // Cyan piscina oscuro (gradientes)
  static const Color secondary = Color(0xFF4DD0E1);       // Cyan piscina claro (acentos)

  static const Color backgroundLight = Color(0xFFE0F7FA); // Fondo piscina muy clarito
  static const Color backgroundDark = Color(0xFFB2EBF2);   // Fondo piscina medio

  static const Color surface = Colors.white;
  static const Color textPrimary = Color(0xFF1E2A32);
  static const Color textSecondary = Color(0xFF7C8A93);

  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);

  static const Color border = Color(0xFFB2EBF2);          // Borde cyan piscina
}